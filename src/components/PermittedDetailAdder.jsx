import React, { PropTypes } from 'react';

const PermittedDetailAdder = (props) => {
  return (
    <div className="permitted-detail-adder">
      //TODO: consolidate permitted-detail-adder blocks to use this component via <PermittedDetailAdder category="food"/>
    </div>
  );
}

export default PermittedDetailAdder;